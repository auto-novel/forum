{
  "name": "forum",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:admin": "pnpm --filter forum-admin dev",
    "dev:web": "pnpm --filter forum-web dev",
    "build": "pnpm --filter forum-web build && pnpm --filter forum-admin build"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
